import { DateMakerDirective } from './date-maker.directive';

describe('DateMakerDirective', () => {
  it('should create an instance', () => {
    const directive = new DateMakerDirective();
    expect(directive).toBeTruthy();
  });
});
